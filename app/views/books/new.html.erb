<h2>New book</h2>

<%= form_for(@book) do |f| %>
  <div class="field">
    <%= f.label :Title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :Body %><br>
    <%= f.text_area :body %>
  </div>

  <%= f.submit 'Create Book' %>

<% end %>
